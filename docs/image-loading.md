# Image Loading

The detailed image architecture and operational guidance is preserved in [Image system details](./image-system.md).

The system includes card-image rendering components, client-side preloading, service-worker caching in `public/sw.js`, and performance monitoring utilities. Update the detailed guide whenever cache behavior, image sources, or preloading strategy changes.
